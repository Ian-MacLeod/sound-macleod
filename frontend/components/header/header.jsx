import React from "react";
import SessionButtons from "./session_buttons_container";

const Header = () => (
  <header>
    <div id="logo" />
    <SessionButtons />
  </header>
);

export default Header;
